package main;

public class TaskOne_CreateARecordAndUseIt {

	/**
	 * 1. Create a record named "Rectangle" with a and b side lengths.
	 * 
	 * 2. Create a second constructor with a single value, which will be used for a
	 * and b (=Square)
	 * 
	 * 3. Check for the canonical and the new constructor, that the entered values
	 * have to be greater then zero. Otherwise throw an exception <br>
	 * 
	 * 4. Create a static method which returns a new record instance with same side lengths (calling the "square" constructor)
	 * 
	 * 5. Create a public method calculating the surface of the record.
	 * 
	 */

}
